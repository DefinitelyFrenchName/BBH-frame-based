# Re-baselines — every time the fidelity contract's expected text moved

One dated line per verdict-text or classifier change that re-baselined a
fidelity row (`docs/conventions.md` 10). Newest first; the newest line is
printed at the head of every `selftest/test_fidelity_vampire.sh` run, so a
re-baseline is never silent. The format is the line below: date, the rows,
what moved and why, the two commits (harness first — it is pushed first).

- 2026-09-25 F1: the classifier resets its controls-reader state at entry on both sides (the lineage's GitHub #140: a FAIL / SKIP / TIMEOUT row kept the previous gate's counts and label), and F1's declaring block gains `g_ffail`, a failing stub right after a declaring gate — the expected readout stays `fired 5 / declared 7` (it read 6 / 8 before the reset); harness and lineage commits of 14z-183.
- 2026-09-15 F9: the gate-index page prose drops the portable tier's runtime — "`tests/run_all_static.sh` (ROM-free, ~1 min)" becomes "(ROM-free)" in the lineage's `tools/gen_gate_index.py` and in this copy (`lib/py/bbh/config.py`), because the lineage measured its portable tier at ~4 min of gates and ~15 min more with controls executed (2026-09-15, the maintainer's run). The lineage's first strict tier after changing only its side went red on F9 alone (`gate-index --check` STALE and the render differing from the committed file), which is the row working as intended; both sides now carry the same text and F9 is identical again. No verdict text, no classifier. Harness `833f7a7` / lineage `e4fbb4a1`.
- 2026-09-15 F1: the normaliser masks the `(costliest controls: …)` list's PICKS and ORDER as well as its figures — each entry `<gate> Ns over <n>` becomes `G Ns over N`, the entry count and format still compared. The lineage's 14z-155 close tier went red on F1 alone (`g_xhon · g_cfired · g_xdied` against `g_cfired · g_xdied · g_xhon`) and green re-run alone: both runners run the same code, and the list is the top three by summed WHOLE seconds, so a control crossing a second boundary in one run reorders it or changes who is on it — the 2026-09-14 line's "every figure inside the duration mask" held for the figures and not for what they select. F1 now also checks the normaliser itself (a reordered pair and a different pick normalise equal, a three-entry list stays distinct from a two-entry one). No runner text changed; the consumer side needs no change. Harness `6e11c48` (this line follows it).
- 2026-09-14 F1: the controls readout gains `  time:     gates Ns  controls Ns  wall Ns  (costliest controls: …)` after the `executed:` line, printed only when controls were executed — the maintainer asked where a strict tier's 38 minutes went, and every control was already timed only for the record to die with the run's temporary directory (a quiet tier: ~20 min of gates, ~17 of executed controls); both runners in one sitting, every figure inside the duration mask, F1 identical over the nine declaring stubs and F2 unchanged (`--exec-controls none`); harness `abeb5c6`, lineage `15389e81`.
- 2026-09-14 F11: the skill-guide RED path is now required to match as well — a copy of the guided skills' files with one GUIDE.md hand-edited, both generators exit 1 on STALE with identical text, only each tool's own regenerate command masked (`tools/gen_skill_guide.py` / `bbh skill-guide`). The maintainer: "Let's require them to match. If and only if this raises issues, we'll reconsider." No existing row's text moved; one row added. Harness `b27b6ee` (this line follows it); the consumer side needs no change.
- 2026-09-11 F1: the runners print `        | (full log: <path>)` under a failing gate's tail — the lineage's static runner and this one both keep a FAILING gate's FULL log now (`[classify] fail_logs`, default `build/gate_failures_static`), after a fidelity gate went red inside the lineage's tier and green alone TWICE with no surviving evidence; the verdict text moved on both sides in one sitting; harness `f9d08a8`, lineage `46ccb3b8`.
- 2026-09-10 F1/F2: the must-fire contract's reader lifted into the harness as a copy (`lib/sh/controls.sh`); the controls readout header became the generic `== must-fire controls ==` on both sides (the lineage's had named its own lib path) and a controls-red exit-0 gate's FAIL row reads `(controls RED: …)` instead of `(exit 0 after a shell error)` on both sides; F1 extended over nine declaring stubs, F2 re-measured identical at 70 rows with `--exec-controls none` on both sides; harness `02d58f3`, lineage `f3019df8`.
- 2026-09-07 F1: the lineage's static runner gained the exit-0-after-shell-error branch (its 14z-139 gave its three runners ONE classifier), so F1 asserts identity on the shell-crash gate instead of the known delta; harness `3eff2d4`, lineage `ae646787`.
